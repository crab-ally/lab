# ========== MuJoCo 시뮬레이터 + robot_state_publisher + twist_mux ==========
1. docker compose up robot
1-1. docker compose exec robot bash


# ==================== RViz ====================

# RViz + SLAM
2. docker compose up rviz_slam

# RViz (저장된 지도) + Nav2
3. docker compose up rviz_nav2


# ==================== Detection ====================

# 화재감지 (fire_detection_node + fire_fusion_node)
4. cd ros2_ws && colcon build --packages-select fire_detection_pkg
4-1. docker compose up fire_detection

# yolo + ppe + sensor fusion + ttc
5. cd ros2_ws && colcon build --packages-select perception_safety_pkg
5-1. docker compose up perception_safety

# 사람의 쓰러짐 감지(Yolo Pose 기반)
6. docker compose up fall_detection

# ==================== 보조기능 ====================

# 로봇 수동 조종 (Teleop)
7. docker compose run --rm teleop
7-1. 전진(i), 우회전(l), 좌회전(j), 정지(k)

# 봇 자동 탐색 주행 실행
8. docker compose run --rm auto_explore

# 지도 저장
9. docker compose run --rm map_saver


# ==================== yolo train ====================

# google colab
yolo detect train \
model=yolov8n.pt \
data=/workspace/datasets/ppe_dataset/data.yaml \
epochs=80 \
imgsz=640 \
batch=16 \
project=/workspace/models \
name=ppe_yolov8n